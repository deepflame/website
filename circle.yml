general:
  artifacts:
    - "build.tar.gz"
test:
  override:
    - bundle exec middleman build
    - tar -czvf $CIRCLE_ARTIFACTS/build.tar.gz build
deployment:
  production:
    branch: master
    commands:
      - bundle exec middleman sync
      - bundle exec middleman invalidate
